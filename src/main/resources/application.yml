server:
  port: 8761

spring:
  application:
    name: discoveryservice
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: ecommerce

eureka:
  client:
    # 자기 자신은 등록하지 않는다.
    register-with-eureka: false
    fetch-registry: false
